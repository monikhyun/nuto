spring:
  application:
    name: Nuto
  config:
    import: classpath:application-secret.yml
  profiles:
    active: secret

  logging:
    level:
      org:
        hibernate:
          SQL: debug
          type:
            descriptor:
              sql:
                BasicBinder: trace